@model mazblog.ViewModels.BlogEntryViewModel
@{
    ViewBag.Title = Model.Title;
}

<div class="page-header">
    <h1>@Model.Title <small>@Model.PublishDate.ToString("D")</small></h1>
</div>
<div class="row">
    @Html.Raw(Model.Content)
</div>